<UserControl x:Class="Dopamine.Views.FullPlayer.Collection.CollectionHome"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Dopamine.Views.FullPlayer.Collection"
      xmlns:baseviews="clr-namespace:Dopamine.Views.Common.Base"
      mc:Ignorable="d" d:DesignWidth="1200" d:DesignHeight="700"
      xmlns:dc="clr-namespace:Digimezzo.Foundation.WPF.Controls;assembly=Digimezzo.Foundation.WPF"
      xmlns:pc="clr-namespace:Dopamine.Controls"
      xmlns:prismMvvm="clr-namespace:Prism.Mvvm;assembly=Prism.Wpf"
      xmlns:prism="http://www.codeplex.com/prism"
      xmlns:enums="clr-namespace:Dopamine.Core.Enums;assembly=Dopamine.Core"
      xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
      prismMvvm:ViewModelLocator.AutoWireViewModel="True"
     >

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <UserControl.Resources>
        <!-- 左侧导航按钮样式 -->
        <Style x:Key="NavRadioButtonStyle" TargetType="RadioButton">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource Brush_PrimaryText}"/>
            <Setter Property="Padding" Value="20,0,0,0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" 
                            Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            Margin="5,2,5,2">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#25FFFFFF"/>
                            </Trigger>

                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#40FFFFFF"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" MinWidth="150" MaxWidth="300"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <!-- ========== 左侧导航栏 ========== -->
        <Border Grid.Column="0" Background="{DynamicResource Brush_PaneBackground}">
            <DockPanel Margin="0,20,0,16">
                <!-- 导航菜单 -->
                <StackPanel Margin="0,0,0,0">
                    <StackPanel.Resources>
                        <DataTemplate x:Key="SafeNavContentTemplate">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Tag, RelativeSource={RelativeSource AncestorType=RadioButton}}" 
                           FontFamily="Segoe MDL2 Assets" 
                           FontSize="15"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>
                                <TextBlock Text="{Binding Content, RelativeSource={RelativeSource AncestorType=RadioButton}}" 
                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </StackPanel.Resources>

                    <TextBlock Text="主页" FontSize="13" Foreground="#A0A0A6" Margin="20,0,0,8" FontWeight="Medium"/>

                    <RadioButton Tag="&#xF246;" Content="概览" 
                 ContentTemplate="{StaticResource SafeNavContentTemplate}"
                 Style="{StaticResource NavRadioButtonStyle}" 
                 IsChecked="True" 
                 Command="{Binding HomeNavigateCommand}" 
                 CommandParameter="{x:Static enums:HomeRegion.Overview}"/>

                    <RadioButton Tag="&#xE71E;" Content="搜索" 
                 ContentTemplate="{StaticResource SafeNavContentTemplate}"
                 Style="{StaticResource NavRadioButtonStyle}"  
                 Command="{Binding HomeNavigateCommand}"  
                 CommandParameter="{x:Static enums:HomeRegion.Search}"/>

                    <TextBlock Text="媒体库" FontSize="13" Foreground="#A0A0A6" Margin="20,20,0,8" FontWeight="Medium"/>

                    <RadioButton Tag="&#xE8D6;" Content="音乐" ContentTemplate="{StaticResource SafeNavContentTemplate}" Style="{StaticResource NavRadioButtonStyle}"/>
                    <RadioButton Tag="&#xE714;" Content="影视" ContentTemplate="{StaticResource SafeNavContentTemplate}" Style="{StaticResource NavRadioButtonStyle}"/>
                    <RadioButton Tag="&#xE7F4;" Content="剧集" ContentTemplate="{StaticResource SafeNavContentTemplate}" Style="{StaticResource NavRadioButtonStyle}"/>
                    <RadioButton Tag="&#xE720;" Content="播客" ContentTemplate="{StaticResource SafeNavContentTemplate}" Style="{StaticResource NavRadioButtonStyle}"/>

                    <TextBlock Text="管理" FontSize="13" Foreground="#A0A0A6" Margin="20,20,0,8" FontWeight="Medium"/>

                    <RadioButton Tag="&#xE8B7;" Content="媒体库管理" ContentTemplate="{StaticResource SafeNavContentTemplate}" Style="{StaticResource NavRadioButtonStyle}"/>
                    <RadioButton Tag="&#xE713;" Content="设置" ContentTemplate="{StaticResource SafeNavContentTemplate}" Style="{StaticResource NavRadioButtonStyle}"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- 拖拽分隔条 -->
        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="{DynamicResource Brush_PaneBackground}"/>

        <!-- 右侧内容区 -->
        <ContentControl Grid.Column="2" x:Name="HomeMainContentRegion"  prism:RegionManager.RegionName="HomeMainContentRegion" />

    </Grid>
</UserControl>